{% extends 'base.html.twig' %}

{% block title %}Etape {{ step }}{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-primary">Suivant</button>
    {{ form_end(form) }}

{% endblock %}
